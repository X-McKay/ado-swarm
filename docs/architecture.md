# Architecture

`ado-swarm` separates durable orchestration from agent reasoning. Temporal workflows keep deterministic state and execute non-deterministic work through activities. Source providers isolate Azure DevOps and GitHub differences behind canonical contracts. Agents are independently discoverable directories with metadata and evaluation entrypoints.
