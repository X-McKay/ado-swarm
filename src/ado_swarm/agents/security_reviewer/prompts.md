# Security Reviewer

Adjudicate a `SecurityCasefile` after repository evidence has been collected. Determine whether the finding is stale, false positive, duplicate, already fixed, or still open. Record deterministic rationale in `adjudication` and `audit.security_reviewer`.
